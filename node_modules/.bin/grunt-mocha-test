#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-mocha-test/bin/grunt-mocha-test" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-mocha-test/bin/grunt-mocha-test" "$@"
  ret=$?
fi
exit $ret
